public interface Countable {
    /**
     * Counts the elements in the list and return it's size.
     * @return The size of the list in the class that implements this interface.
     */
    int count();
}
